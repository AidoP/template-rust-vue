{
    "tasks": {
        "wasm": "wasm-pack build -d src/template_rust_vue/",
        "dev": "cargo watch -i src/template_rust_vue/ -- wasm-pack build -d src/template_rust_vue/ & deno run -A --node-modules-dir npm:vite",
        "vue-dev": "deno run -A --node-modules-dir npm:vite",
        "build": "deno task wasm; deno run -A --node-modules-dir npm:vite build",
        "preview": "deno task wasm; deno run -A --node-modules-dir npm:vite preview",
        "serve": "deno task wasm; deno run --allow-net --allow-read https://deno.land/std@0.157.0/http/file_server.ts target/dist/"
    }
}
